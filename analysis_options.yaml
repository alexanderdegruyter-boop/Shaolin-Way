include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: false

analyzer:
  errors:
    # Markdown package deprecations should not block a personal build.
    deprecated_member_use: ignore
